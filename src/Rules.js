"use strict";
//import DicesGenerator from "./DicesGenerator.js";

export default class Rules {


    /**

        ( number of dice + bonus dices )
        then we need to sum all dices removing bonus-dices and penalty dices 

    */

    // getRollsValueWithModifier( nr, br, pr  ) 
    // {

    //     if ( nr<=0 || pr <=0 ) {
    //         throw ('error nr or pr is lower then 0');
    //     }


    //     var dices = nr+br;
    //     var values = Dice.getDicesRoll(dices, 6);
    //     values.sort( function(a, b){return b-a} );
    //     values.splice( 0, -1*pr + -1*br );
    //     return values;

    // }

}